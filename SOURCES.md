# 数据来源与可追溯性

## 1. 正式历史净值主源

```text
https://fundmobapi.eastmoney.com/FundMNewApi/FundMNHisNetList
```

聚势项目将其作为历史净值的主适配方向，按基金代码和页码增量拉取。常见字段包括：`FSRQ`（净值日期）、`DWJZ`（单位净值）、`JZZZL`（日涨跌幅）和 `LJJZ`（累计净值）。

接入时必须记录请求时间、基金代码、页码、覆盖日期、返回行数和字段校验结果；不能因为接口失败就把旧缓存标记为最新数据。

## 2. 盘中估值参考源

```text
https://fundcomapi.tiantianfunds.com/mm/newCore/FundValuationLast
```

盘中估值可用于界面上的“今日参考”，不能替代收盘后正式净值。QDII、跨市场基金和存在时差的资产尤其需要显示估值不确定性，不能把估值直接用于最终收益结算或回测。

## 3. 历史净值备用源

```text
https://fund.eastmoney.com/pingzhongdata/{fund_code}.js
```

该公开 JS 数据集是 [real-time-fund](https://github.com/hzm0321/real-time-fund) 等同类项目使用过的备用适配方向。备用源可能存在字段变化或数据截断，必须重新校验日期、数值和覆盖范围，并在结果中保留 `source=fallback`。

## 4. 基金目录与披露期重仓

聚势产品可通过 [AkShare](https://github.com/akfamily/akshare) 的公开数据适配基金代码、名称和类型，也可读取基金公开披露的重仓信息。重仓数据必须保留报告期和来源，只能描述“最近披露的重仓”，不能解释成用户当前的实时持仓。

## 5. 缓存与降级顺序

```text
正式历史净值主源 → 已声明的历史备用源 → 带时间戳的本地缓存 → 数据不足
```

缓存只能在界面中标记为缓存，并展示最后更新时间。所有源都不可用时，系统应返回“数据不足”，而不是返回演示数据或推测值。

## 6. 数据安全边界

- 不抓取或保存支付宝、微信登录凭证、Cookie、Token 或个人账号信息。
- 用户截图只应在本地 OCR 链路中处理，不进入本开源仓库。
- 不提交 API Key、代理配置、用户持仓和交易记录。
- 外部接口的可用性、频率限制和字段含义以服务提供方为准。
